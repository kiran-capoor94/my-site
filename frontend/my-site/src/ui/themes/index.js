// src/ui/theme/index.js

import { createMuiTheme } from '@material-ui/core/styles';

const palette = {
  primary: { main: '#212121' },
  secondary: { main: '#D50000' }
};
const themeName = 'Mine Shaft Guardsman Red Geese';

export default createMuiTheme({ palette, themeName });
